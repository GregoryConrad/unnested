/// TODO description
library unnested;

import 'src/unmodifiable_recursive_builder.dart';

// Unnested is user-defined & specified in a user config file.
// The builder class would be generated, or necessary components
// would be added onto the Unnested class itself
class Unnested extends UnmodifiableRecursiveBuilder {}
